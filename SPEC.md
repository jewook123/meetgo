# MeetGo 기능 명세서 (MVP)

## 1. 개요

| 항목 | 내용 |
|---|---|
| 서비스명 | MeetGo |
| 유형 | 소셜/매칭 (데이팅) 서비스 |
| 대상 플랫폼 | Android |
| 기술 스택 | Kotlin, Jetpack Compose, Navigation Compose, Material 3 |
| 개발 단계 | 프론트엔드 우선 구현 (Mock 데이터), 백엔드는 추후 연동 |

Figma 와이어프레임 없이, 본 명세서를 기준으로 화면 구조와 동작을 정의하고 이를 바탕으로 Compose 화면을 구현한다.

## 2. 화면 목록

| # | 화면 | 라우트 | 설명 |
|---|---|---|---|
| 1 | 온보딩 | `onboarding` | 앱 최초 실행 시 서비스 소개 |
| 2 | 회원가입 | `signup` | 이메일 기반 신규 가입 |
| 3 | 로그인 | `login` | 이메일/비밀번호 로그인 |
| 4 | 프로필 설정 | `profile_setup` | 가입 직후 프로필 등록 |
| 5 | 탐색(피드) | `discover` | 거리순 프로필 리스트 |
| 6 | 상대 프로필 상세 | `profile_detail/{userId}` | 탐색에서 프로필 클릭 시 상세 |
| 7 | 매칭 목록 | `matches` | 성립된 매칭 + 대화 리스트 |
| 8 | 채팅 | `chat/{matchId}` | 1:1 채팅 |
| 9 | 마이프로필 | `my_profile` | 내 프로필 조회/수정 |
| 10 | 신고/차단 | 채팅·프로필 내 다이얼로그 | 부적절한 상대 신고/차단 |

하단 탭(Bottom Navigation): **탐색 / 매칭 / 마이프로필** 3개 고정.
온보딩·회원가입·로그인·프로필설정·채팅·프로필상세는 탭 바 없이 별도 스택으로 진행.

## 3. 사용자 흐름

```
온보딩 → 로그인 ⇄ 회원가입 → (최초 1회) 프로필 설정 → 탐색(홈)
탐색 → 프로필 상세 → 좋아요/스킵
  └ 상호 좋아요 성립 시 → 매칭 완료 다이얼로그 → 매칭 목록에 추가
매칭 목록 → 채팅방 입장 → 채팅(텍스트/사진) → (필요시) 신고/차단
마이프로필 → 프로필 수정 / 로그아웃
```

- 매칭은 **상호 관심(좋아요) 확인 시에만 성립**하며, 매칭 성립 전에는 채팅을 시작할 수 없다.
- 로그인하지 않은 상태에서는 탐색/매칭/마이프로필에 접근할 수 없다.

## 4. 화면별 상세 명세

### 4.1 온보딩
- 앱 로고 및 슬로건, "시작하기" 버튼 → 로그인 화면 이동
- 이미 로그인 세션이 있으면 온보딩을 건너뛰고 탐색으로 바로 이동 (Mock 단계에서는 스킵 로직만 자리 표시)

### 4.2 회원가입
필수 입력 항목:
- 이름/닉네임
- 이메일
- 비밀번호 (+비밀번호 확인)
- 생년월일 (만 나이 계산, 미성년자 가입 제한)
- 성별
- 프로필 사진 최소 1장 (미등록 시 다음 단계 진행 불가)

가입 완료 시 프로필 설정(추가 정보) 화면으로 이동.

### 4.3 로그인
- 이메일 + 비밀번호 입력, "로그인" 버튼
- "비밀번호를 잊으셨나요?" (Mock에서는 자리 표시, 동작 미구현)
- 하단에 "회원가입" 이동 링크

### 4.4 프로필 설정
- 자기소개(bio) 텍스트
- 관심사 태그 선택 (다중 선택, 예: 등산/카페/영화/러닝 등)
- 사진 추가 등록 (최대 6장, 다중 업로드)
- "완료" → 탐색 화면으로 이동

### 4.5 탐색(피드)
- **리스트/피드형** UI (세로 스크롤 카드 리스트, 스와이프 카드 아님)
- **기본 정렬: 거리순(가까운 순)** — 각 카드에 대략적인 거리(예: "2km") 표시
- 카드 구성: 대표 사진, 이름, 나이, 거리, 짧은 소개
- 카드 클릭 → 프로필 상세로 이동
- 카드 내 "좋아요" / "스킵" 액션 버튼 제공
- 좋아요 시 상대도 나를 좋아요 했다면 → **매칭 성립 다이얼로그** 표시 ("채팅 시작하기" / "계속 둘러보기")

### 4.6 상대 프로필 상세
- 사진 전체(여러 장 스와이프), 이름/나이/거리, 소개, 관심사 태그
- 하단 고정 액션: 좋아요 / 스킵
- 신고 버튼 (우측 상단 메뉴)

### 4.7 매칭 목록
- 매칭 성립된 상대 리스트 (프로필 사진, 이름, 마지막 메시지 미리보기, 안읽음 표시)
- 항목 클릭 → 채팅 화면 이동
- 매칭은 있으나 아직 대화가 없는 경우 "대화를 시작해보세요" 안내

### 4.8 채팅
- 상호 매칭된 상대와만 진행 (매칭 전 채팅 진입 불가)
- 텍스트 메시지 전송
- **사진 전송** 지원 (첨부 아이콘 → 갤러리에서 선택)
- 상단 바: 상대 이름/사진, 뒤로가기, 메뉴(신고/차단/매칭 해제)

### 4.9 마이프로필
- 내 사진, 이름, 나이, 소개, 관심사 표시
- "프로필 수정" → 프로필 설정 화면 재사용(수정 모드)
- 설정: 로그아웃, 계정 관리 (자리 표시)

### 4.10 신고/차단
- 채팅방 메뉴 및 프로필 상세에서 진입
- 신고 사유 선택(스팸/부적절한 사진/사기 의심/기타) + 상세 사유 입력
- 차단 시 즉시 매칭 목록/탐색에서 상대 제외 (Mock 상태에서는 로컬 필터링)

## 5. 데이터 모델 (Mock 기준)

```kotlin
UserProfile(id, name, age, gender, bio, photos: List<String>, interests: List<String>, distanceKm: Double)
MatchPreview(id, otherUser: UserProfile, lastMessage: String, isUnread: Boolean, matchedAt: Long)
ChatMessage(id, matchId, text: String?, photoUrl: String?, isMine: Boolean, sentAt: Long)
ReportReason(SPAM, INAPPROPRIATE_PHOTO, SUSPECTED_FRAUD, OTHER)
```

가입/로그인 관련 인증 상태는 우선 로컬 in-memory 세션(Mock Repository)으로 관리하고,
추후 실제 백엔드 API로 교체할 수 있도록 Repository 인터페이스로 분리한다.

## 6. MVP 범위 / 추후 확장

**이번 MVP에 포함**
- 위 10개 화면 전체 (Mock 데이터 기반)
- 이메일 회원가입/로그인, 상호 매칭 로직, 리스트형 탐색(거리순), 채팅(텍스트+사진), 신고/차단

**추후 확장 (이번 범위 아님)**
- 실제 백엔드/DB 연동, 소셜 로그인
- 푸시 알림
- 결제/구독(프리미엄) 기능
- 실시간 위치 기반 정밀 거리 계산 (MVP는 Mock 값 사용)

## 7. 비고
- 실제 Figma 와이어프레임이 추후 제공되면, 본 명세서의 정보 구조(IA)는 유지한 채 레이아웃/스타일만 맞춰 조정한다.
